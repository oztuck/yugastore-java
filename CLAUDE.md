@AGENTS.md

Everything below is Claude-specific; everything the whole team needs (build,
run, architecture, gotchas) lives in `AGENTS.md` above so it stays usable by
Copilot, Codex, and Duo too. Add to `AGENTS.md` by default — only put
something here if it's genuinely Claude-only.
